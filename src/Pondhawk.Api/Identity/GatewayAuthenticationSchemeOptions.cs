using Microsoft.AspNetCore.Authentication;

namespace Pondhawk.Api.Identity;

/// <summary>Options for the gateway authentication scheme (shared by the header and token handlers).</summary>
public sealed class GatewayAuthenticationSchemeOptions : AuthenticationSchemeOptions
{
}
